"""


For each category of problem:
  For each problem in category:
    For each theme:
      For each hyperparam (model, temp)
        Generate Problem

Evaluate(problem) -> {model/hyperparm: botched?}

output as a spreadsheet so we can go over in google sheets

"""